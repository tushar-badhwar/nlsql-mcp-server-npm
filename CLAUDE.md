# Claude — read this first

This is a **git worktree** of `nlsql-mcp-server-npm` on branch `week2-claude-adapter`. The driver worktree (on `main`) is at `../nlsql-mcp-server-npm/`.

**Before doing anything**, read `SETUP.md` in this directory — it specifies:
- One-time setup (venv, BIRD dataset symlink)
- The concrete goal of this worktree (Claude Agent SDK adapter + MCP server + BIRD-mini run)
- What NOT to do here (BIRD scorer is being worked on in a separate worktree; no `kit/` framework extraction)

For broader project context, read `../nlsql-mcp-server-npm/v1_plan.md` and `../nlsql-mcp-server-npm/v0_proposal.md`.
